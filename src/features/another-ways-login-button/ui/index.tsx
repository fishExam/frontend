import { Button } from '@/shared';

export const AnotherWaysLoginButton = () => {
  return (
    <Button variant="secondary" className="w-full font-bold text-secondary-foreground">
      Другие способы
    </Button>
  );
};
